<div class="mb-4">
  <%= render "shared/go_back" %>
</div>

<div class="mb-6">
  <h1 class="text-3xl font-bold tracking-tight text-gray-900">
    Share Password
  </h1>
  <p class="mt-2 text-sm text-gray-600">
    Grant access to <span class="font-medium text-indigo-600"><%= @password.url %></span> to another user.
  </p>
</div>

<!-- Render the Form Partial -->
<%= render "form",
           password: @password,
           user_password: @user_password %>

